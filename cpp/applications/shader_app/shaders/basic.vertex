#version 330
uniform mat4 ProjectionMatrix;
layout(location = 0) in vec3 vertexPosition_modelspace;


void main()
{
    // gl_Position.xyz = vertexPosition_modelspace;
    // gl_Position.w = 1.0;
    gl_Position = ProjectionMatrix * vec4(vertexPosition_modelspace, 1.0);
}