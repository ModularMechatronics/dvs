 * Add wxStaticBox that can contain other GUI elements
 * Add radio button group
    * Rename CheckBox to Checkbox(lower case 'b' on box), and corresponding snake cases if there are any
 * Rename DropDownMenu to DropdownMenu? (and corresponding snake cases)