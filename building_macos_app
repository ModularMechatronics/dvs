# Generate XCode project with
cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_OSX_ARCHITECTURES="x86_64" -G Xcode ..

For the target "dvs", set the correct target version of macOS.

Build the dvs application.

Export the binary to the duoplot.app/Contents/MacOS folder

# Then run
xattr -rc /Applications/duoplot.app


For seeing how to include an icon, build the wxWidgets example "shaped", and see how the shaped.app is structured
